<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">

<properties>
    <entry key="getFolioByReference">
        SELECT p.FOLIO
        FROM SERVICE_CHARGE_DETAIL sc INNER JOIN PAYMENT p
        ON sc.ID = p.ID_SERVICE_CHARGE
        WHERE sc.REFERENCE_PAYMENT = ?
        AND p.ID_STATUS_PAYMENT = 3
        ORDER BY p.FOLIO DESC LIMIT 1;
    </entry>
</properties>