spring:
  application:
    name: kyc-batchs
  profiles:
    active: dev
  batch:
    job:
      enabled: true

logging:
  level:
    root: INFO
    com.kyc: INFO
    org.springframework: INFO
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql: TRACE

